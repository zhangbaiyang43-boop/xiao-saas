# AppTag

Foundation primitive for compact status and metadata tags.

## API

- `tone`: `brand | warning | danger | neutral`, default `neutral`
- `size`: `sm | md`, default `sm`
- `subtle`: light surface mode, default `true`

Use for visual labels such as store status, new item, table context, dine-in, and coupon count. It has no business logic.
