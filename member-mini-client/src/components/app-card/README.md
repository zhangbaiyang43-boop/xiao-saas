# AppCard

Foundation primitive for a simple white card surface.

## Contract

- Uses `--bg-card`, `--radius-card`, and `--card-shadow`.
- Owns only surface styling.
- Does not include business title, icon, button, list, or state logic.
- Should not be nested inside another card.
